{
  "watch": [
    "src/modules",
    "src/electron",
    "../../packages/openapi-schema",
    "../../packages/common-types",
    "../../packages/skill-template",
    "../../packages/utils"
  ],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "prisma generate && tsc --build tsconfig.electron.json && MODE=desktop electron ."
}
