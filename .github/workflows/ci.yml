name: CI

on:
  pull_request:
    types: [opened, synchronize]
    paths-ignore:
      - 'docs/**'

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit
    with:
      environment: 'ci'
